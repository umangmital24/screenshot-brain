
/* ---------- Scroll journey clarity refinements ---------- */
.memory-journey-step:last-child {
  min-height: 76vh;
  padding-bottom: 10vh;
}

.journey-bubble-label {
  position: absolute;
  right: 66px;
  top: 310px;
  z-index: 7;
  font-size: 9px;
  font-weight: 700;
  color: #18181b;
  background: rgba(255,255,255,0.96);
  border: 1px solid #e4e4e7;
  border-radius: 999px;
  padding: 6px 9px;
  box-shadow: 0 8px 20px -14px rgba(0,0,0,0.3);
  transition: opacity 0.28s ease, transform 0.35s ease;
}

.journey-samhaal-bubble {
  width: 50px;
  height: 50px;
  right: 16px;
  top: 294px;
  font-size: 14px;
  border: 2px solid rgba(255,255,255,0.96);
}

.journey-bubble-s,
.journey-bubble-check {
  position: absolute;
  inset: 0;
  display: grid;
  place-items: center;
  transition: opacity 0.24s ease, transform 0.32s cubic-bezier(0.22,1,0.36,1);
}

.journey-bubble-check {
  opacity: 0;
  transform: scale(0.55);
  font-size: 20px;
}

.journey-save-confirm {
  position: absolute;
  left: 50%;
  top: 118px;
  z-index: 9;
  width: 214px;
  transform: translate(-50%, 12px) scale(0.96);
  opacity: 0;
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 11px 12px;
  border-radius: 14px;
  background: rgba(255,255,255,0.98);
  border: 1px solid #dedee3;
  box-shadow: 0 16px 36px -22px rgba(0,0,0,0.42);
  transition: opacity 0.32s ease, transform 0.42s cubic-bezier(0.22,1,0.36,1);
}

.journey-confirm-mark {
  flex: 0 0 30px;
  width: 30px;
  height: 30px;
  border-radius: 50%;
  display: grid;
  place-items: center;
  background: #09090b;
  color: #fff;
  font-size: 15px;
  font-weight: 800;
}

.journey-save-confirm div {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.journey-save-confirm strong {
  font-size: 10px;
  color: #18181b;
}

.journey-save-confirm div span {
  font-size: 8px;
  color: #71717a;
}

.journey-memory-card {
  inset: 72px 18px auto;
  padding: 20px;
  min-height: 410px;
  box-shadow: 0 24px 48px -30px rgba(0,0,0,0.42);
}

.journey-memory-success {
  display: flex;
  align-items: center;
  gap: 7px;
  margin-bottom: 18px;
  font-size: 9px;
  color: #52525b;
}

.journey-memory-success span {
  width: 22px;
  height: 22px;
  border-radius: 50%;
  display: grid;
  place-items: center;
  background: #09090b;
  color: #fff;
  font-size: 10px;
}

.journey-memory-success strong {
  font-size: 9px;
  color: #18181b;
}

.journey-memory-topline {
  margin-bottom: 34px;
}

.journey-memory-card > strong {
  display: block;
  font-size: 29px;
  line-height: 1.02;
}

.journey-memory-card > p {
  font-size: 12px;
  max-width: 210px;
}

.journey-memory-meta {
  margin-top: 30px;
}

.journey-ask-view {
  inset: 70px 18px auto;
  padding: 20px;
  min-height: 430px;
}

.journey-ask-brand {
  font-size: 13px;
  padding-bottom: 17px;
}

.journey-user-question {
  margin-top: 36px;
  font-size: 12px;
  padding: 14px 15px;
}

.journey-answer p {
  font-size: 12px;
  line-height: 1.65;
}

.journey-source-chip {
  font-size: 9px;
  display: inline-flex;
}

/* Capture is visually explicit. */
.journey-phone-screen.is-step-0 .journey-samhaal-bubble {
  animation: journey-bubble-pulse 1.8s ease-in-out infinite;
}

/* After capture, the bubble becomes a clear check and confirmation appears. */
.journey-phone-screen.is-step-1 .journey-bubble-label {
  opacity: 0;
  transform: translateX(8px);
}

.journey-phone-screen.is-step-1 .journey-bubble-s {
  opacity: 0;
  transform: scale(0.55);
}

.journey-phone-screen.is-step-1 .journey-bubble-check {
  opacity: 1;
  transform: scale(1);
}

.journey-phone-screen.is-step-1 .journey-samhaal-bubble {
  transform: scale(0.9);
}

.journey-phone-screen.is-step-1 .journey-save-confirm {
  opacity: 1;
  transform: translate(-50%, 0) scale(1);
}

/* Organize: remove capture UI so the memory card is unmistakably the focus. */
.journey-phone-screen.is-step-2 .journey-bubble-label,
.journey-phone-screen.is-step-2 .journey-save-confirm {
  opacity: 0;
}

.journey-phone-screen.is-step-2 .journey-samhaal-bubble {
  opacity: 0;
}

.journey-phone-screen.is-step-2 .journey-capture-card {
  opacity: 0.06;
  transform: translateY(-10px) scale(0.9);
  filter: blur(2px);
}

.journey-phone-screen.is-step-2 .journey-memory-card {
  opacity: 1;
  transform: translateY(0) scale(1);
}

/* Ask stays visible for the full final scroll beat. */
.journey-phone-screen.is-step-3 .journey-bubble-label,
.journey-phone-screen.is-step-3 .journey-save-confirm {
  opacity: 0;
}

@keyframes journey-bubble-pulse {
  0%, 100% { box-shadow: 0 10px 20px rgba(0,0,0,0.2), 0 0 0 0 rgba(9,9,11,0); }
  50% { box-shadow: 0 10px 20px rgba(0,0,0,0.2), 0 0 0 7px rgba(9,9,11,0.08); }
}

@media (max-width: 900px) {
  .memory-journey {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0;
  }

  .memory-journey-visual {
    position: sticky;
    top: 62px;
    z-index: 5;
    order: 0;
    min-height: 390px;
    height: 390px;
    padding: 4px 0;
    background: linear-gradient(to bottom, rgba(250,250,250,0.98) 84%, rgba(250,250,250,0));
    pointer-events: none;
  }

  .journey-phone-wrap {
    transform: scale(0.58);
    transform-origin: top center;
    height: 390px;
  }

  .journey-phone {
    width: 310px;
    height: 628px;
  }

  .journey-stage-caption {
    margin-top: 8px;
  }

  .memory-journey-copy {
    order: 1;
    padding-left: 28px;
    position: relative;
    z-index: 2;
  }

  .memory-journey-step {
    min-height: 72vh;
    padding: 16vh 0 14vh;
    opacity: 0.28;
    transform: translateY(8px);
  }

  .memory-journey-step.is-active {
    opacity: 1;
    transform: translateY(0);
  }

  .memory-journey-step:last-child {
    min-height: 88vh;
    padding-bottom: 24vh;
  }
}

@media (max-width: 520px) {
  .memory-journey-visual {
    height: 350px;
    min-height: 350px;
  }

  .journey-phone-wrap {
    transform: scale(0.52);
    height: 350px;
  }
}

@media (prefers-reduced-motion: reduce) {
  .journey-phone-screen.is-step-0 .journey-samhaal-bubble {
    animation: none !important;
  }
}
