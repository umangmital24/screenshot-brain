  flex-direction: column;
  gap: 6px;
  box-shadow: var(--shadow-subtle);
}

.tip-kbd {
  font-family: var(--font-mono);
  font-size: 11px;
  font-weight: 600;
  color: var(--accent-black);
  display: flex;
  align-items: center;
  gap: 4px;
}

.tip-text {
  font-size: 11.5px;
  color: var(--text-secondary);
  line-height: 1.45;
  margin: 0;
}

.tip-text em {
  font-style: normal;
  color: var(--text-primary);
}

/* ---------- Universal Modal Overlay & Dialogs ---------- */
.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.48);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24px;
  animation: fadeIn 0.2s ease-out;
}

.modal-close-btn {
  width: 30px;
  height: 30px;
  border-radius: var(--radius-xs);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--text-muted);
  border: 1px solid var(--border-subtle);
  background: #ffffff;
  cursor: pointer;
  transition: all 0.18s ease;
}

.modal-close-btn:hover {
  background: var(--bg-muted);
  color: var(--accent-black);
  border-color: var(--border-hover);
}

/* Lightbox Modal */
.lightbox-modal {
  width: 100%;
  max-width: 820px;
  max-height: 90vh;
  background: #ffffff;
  border: 1px solid var(--border-default);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-modal);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  animation: fadeInUp 0.3s var(--ease-spring);
}

.lightbox-topbar {
  padding: 14px 20px;
  border-bottom: 1px solid var(--border-subtle);
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: #fafafa;
}

.lightbox-badge-group {
  display: flex;
  align-items: center;
  gap: 10px;
  overflow: hidden;
}

.lightbox-title {
  font-size: 14px;
  font-weight: 600;
  color: var(--text-primary);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.lightbox-view-area {
  padding: 20px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #f4f4f5;
  overflow-y: auto;
  max-height: 60vh;
}

.lightbox-view-area img {
  max-width: 100%;
  max-height: 56vh;
  object-fit: contain;
  border-radius: var(--radius-sm);
  box-shadow: var(--shadow-card);
}

.lightbox-footer-info {
  padding: 16px 20px;
  border-top: 1px solid var(--border-subtle);
  background: #ffffff;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.lightbox-extracted-text {
  font-family: var(--font-mono);
  font-size: 12px;
  color: var(--text-secondary);
  line-height: 1.55;
  max-height: 100px;
  overflow-y: auto;
  background: #f9fafb;
  border: 1px solid var(--border-subtle);
  padding: 10px 12px;
  border-radius: var(--radius-xs);
  white-space: pre-wrap;
}

/* Shortcuts Dialog */
.shortcuts-dialog {
  width: 100%;
  max-width: 440px;
  background: #ffffff;
  border: 1px solid var(--border-default);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-modal);
  padding: 24px;
  display: flex;
  flex-direction: column;
  gap: 18px;
  animation: fadeInUp 0.3s var(--ease-spring);
}

.shortcuts-topbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.shortcuts-title {
  font-size: 16px;
  font-weight: 600;
  color: var(--text-primary);
}

.shortcuts-grid {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.shortcut-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 8px 12px;
  background: #fafafa;
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-xs);
  font-size: 13px;
  color: var(--text-secondary);
}

.shortcut-item kbd {
  font-family: var(--font-mono);
  font-size: 11px;
  font-weight: 600;
  background: #ffffff;
  border: 1px solid var(--border-default);
  border-radius: 4px;
  padding: 2px 7px;
  color: var(--accent-black);
  box-shadow: var(--shadow-subtle);
}

/* Pro Pricing Dialog Elements */
.pricing-header {
  margin-bottom: 12px;
}

.plan-name {
  font-size: 15px;
  font-weight: 600;
  color: var(--accent-black);
  display: block;
  margin-bottom: 4px;
}

.plan-price {
  font-size: 26px;
  font-weight: 700;
  letter-spacing: -0.03em;
  color: var(--accent-black);
}

.price-period {
  font-size: 13px;
  font-weight: 400;
  color: var(--text-muted);
}

.plan-features {
  list-style: none;
  padding: 0;
  margin: 14px 0 20px;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.plan-features li {
  font-size: 12.5px;
  color: var(--text-secondary);
  line-height: 1.45;
}

.plan-btn {
  width: 100%;
  padding: 10px 16px;
  border-radius: var(--radius-sm);
  font-size: 13px;
  font-weight: 550;
  border: none;
  cursor: pointer;
  transition: all 0.2s var(--ease-spring);
}

.plan-btn.is-primary {
  background: var(--accent-black);
  color: #ffffff;
  box-shadow: var(--shadow-btn);
}

.plan-btn.is-primary:hover {
  background: #27272a;
  transform: translateY(-1.5px);
  box-shadow: var(--shadow-btn-hover);
}

.plan-btn:disabled {
  opacity: 0.5;
  cursor: wait;
}

/* ---------- Modern Floating Ask AI Copilot Dock ---------- */

.ai-assistant-dock {
  position: fixed;
  bottom: 24px;
  right: 24px;
  z-index: 950;
  display: flex;
  flex-direction: column;
  align-items: flex-end;
}

/* Floating Collapsed Pill Trigger */
.ai-float-trigger-btn {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  background: #09090b;
  color: #ffffff;
  border: 1px solid rgba(255, 255, 255, 0.15);
  border-radius: 9999px;
  padding: 10px 18px 10px 14px;
  cursor: pointer;
  box-shadow: 0 10px 30px -5px rgba(0, 0, 0, 0.35), 0 0 0 1px rgba(0, 0, 0, 0.1);
  transition: all 0.25s var(--ease-spring);
}

.ai-float-trigger-btn:hover {
  transform: translateY(-2.5px) scale(1.02);
  box-shadow: 0 16px 36px -6px rgba(0, 0, 0, 0.45);
  background: #18181b;
}

.ai-float-sparkle {
  width: 24px;
  height: 24px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.12);
  display: flex;
  align-items: center;
  justify-content: center;
  color: #ffffff;
}

.ai-float-title {
  font-size: 13px;
  font-weight: 600;
  letter-spacing: -0.01em;
}

.ai-float-badge {
  font-family: var(--font-mono);
  font-size: 10.5px;
  background: rgba(255, 255, 255, 0.18);
  color: #ffffff;
  padding: 2px 7px;
  border-radius: 9999px;
}

/* Expanded Card Window */
.ai-assistant-card {
  width: 440px;
  max-width: calc(100vw - 32px);
  height: 560px;
  max-height: calc(100vh - 100px);
  background: #ffffff;
  border: 1px solid var(--border-default);
  border-radius: 18px;
  box-shadow: 0 24px 60px -12px rgba(0, 0, 0, 0.25), 0 0 0 1px rgba(0, 0, 0, 0.08);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  animation: fadeInUp 0.28s var(--ease-spring);
}

/* AI Header */
.ai-assistant-header {
  padding: 14px 18px;
  background: #fafafa;
  border-bottom: 1px solid var(--border-subtle);
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.ai-header-left {
  display: flex;
  align-items: center;
  gap: 10px;
}

.ai-sparkle-icon {
  width: 32px;
  height: 32px;
  border-radius: 9px;
  background: var(--accent-black);
  color: #ffffff;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: var(--shadow-subtle);
}

.ai-header-title {
  font-size: 14px;
  font-weight: 600;
  letter-spacing: -0.015em;
  color: var(--accent-black);
  margin: 0 0 1px;
}

.ai-header-sub {
  font-size: 11px;
  color: var(--text-muted);
  margin: 0;
}

.ai-header-actions {
  display: flex;
  align-items: center;
  gap: 8px;
}

.ai-count-pill {
  font-family: var(--font-mono);
  font-size: 10.5px;
  background: #f4f4f5;
  border: 1px solid var(--border-subtle);
  color: var(--text-secondary);
  padding: 2.5px 8px;
  border-radius: 9999px;
}

.ai-close-btn {
  width: 28px;
  height: 28px;
  border-radius: var(--radius-xs);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--text-muted);
  border: 1px solid var(--border-subtle);
  background: #ffffff;
  cursor: pointer;
  transition: all 0.15s ease;
}

.ai-close-btn:hover {
  background: var(--bg-muted);
  color: var(--accent-black);
}

/* AI Body / Stream Area */
.ai-assistant-body {
  flex: 1;
  overflow-y: auto;
  padding: 16px;
  background: #ffffff;
  display: flex;
  flex-direction: column;
}

/* Starter Blank View */
.ai-starter-view {
  margin: auto 0;
  padding: 12px 6px;
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.ai-starter-icon {
  width: 44px;
  height: 44px;
  border-radius: 12px;
  background: #f4f4f5;
  border: 1px solid var(--border-subtle);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent-black);
  margin-bottom: 12px;
}

.ai-starter-heading {
  font-size: 15px;
  font-weight: 600;
  letter-spacing: -0.01em;
  color: var(--text-primary);
  margin-bottom: 6px;
}

.ai-starter-copy {
  font-size: 12px;
  color: var(--text-secondary);
  line-height: 1.5;
  max-width: 340px;
  margin: 0 auto 16px;
}

.ai-chips-label {
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--text-muted);
  margin-bottom: 8px;
}

.ai-chips-row {
  display: flex;
  flex-direction: column;
  gap: 7px;
  width: 100%;
}

.ai-chip {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: #fafafa;
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-sm);
  padding: 8px 12px;
  font-size: 12px;
  color: var(--text-secondary);
  text-align: left;
  cursor: pointer;
  transition: all 0.18s ease;
}

.ai-chip:hover {
  background: #ffffff;
  border-color: var(--border-hover);
  color: var(--accent-black);
  transform: translateX(2px);
  box-shadow: var(--shadow-subtle);
}

.ai-chip-arrow {
  color: var(--text-muted);
  font-family: var(--font-mono);
  font-size: 12px;
  transition: transform 0.15s ease;
}

.ai-chip:hover .ai-chip-arrow {
  transform: translateX(2px);
  color: var(--accent-black);
}

/* Chat Stream */
.ai-log-stream {
  display: flex;
  flex-direction: column;
  gap: 16px;
  flex: 1;
}

.ai-msg-group {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.ai-user-bubble {
  align-self: flex-end;
  background: var(--accent-black);
  color: #ffffff;
  border-radius: 14px 14px 2px 14px;
  padding: 9px 14px;
  font-size: 12.5px;
  line-height: 1.45;
  max-width: 82%;
  box-shadow: var(--shadow-subtle);
}

.ai-user-bubble p {
  margin: 0;
}

.ai-response-bubble {
  align-self: flex-start;
  background: #f9fafb;
  border: 1px solid var(--border-default);
  border-radius: 2px 14px 14px 14px;
  padding: 12px 14px;
  font-size: 12.5px;
  color: var(--text-secondary);
  line-height: 1.55;
  max-width: 92%;
  box-shadow: var(--shadow-card);
}

.ai-bot-badge {
  display: inline-flex;
  align-items: center;
  gap: 5px;
  font-size: 10.5px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.04em;
  color: var(--accent-black);
  margin-bottom: 6px;
}

.ai-bot-badge svg {
  width: 12px;
  height: 12px;
}

.ai-answer-text {
  margin: 0;
  color: var(--text-primary);
  white-space: pre-wrap;
}

/* Referenced Screenshot Sources */
.ai-sources-section {
  margin-top: 10px;
  padding-top: 10px;
  border-top: 1px solid var(--border-subtle);
}

.ai-sources-title {
  font-size: 10.5px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.04em;
  color: var(--text-muted);
  display: block;
  margin-bottom: 6px;
}

.ai-sources-row {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
}

.ai-source-chip {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  background: #ffffff;
  border: 1px solid var(--border-default);
  border-radius: var(--radius-xs);
  padding: 3px 8px 3px 4px;
  cursor: pointer;
  box-shadow: var(--shadow-subtle);
  transition: all 0.15s ease;
}

.ai-source-chip:hover {
  border-color: var(--accent-black);
  transform: translateY(-1px);
  box-shadow: var(--shadow-card);
}

.ai-source-chip img {
  width: 22px;
  height: 22px;
  object-fit: cover;
  border-radius: 2px;
}

.source-thumb-fallback {
  width: 22px;
  height: 22px;
  background: #f4f4f5;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 2px;
  color: var(--text-muted);
}

.source-chip-name {
  font-size: 11px;
  font-weight: 500;
  color: var(--text-primary);
  max-width: 140px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* Thinking Loading State */
.ai-response-bubble.is-loading {
  background: #fafafa;
}

.ai-thinking-row {
  display: flex;
  align-items: center;
  gap: 6px;
}

.ai-thinking-dot {
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: var(--accent-black);
  animation: aiDotPulse 1.4s infinite ease-in-out both;
}

.ai-thinking-dot:nth-child(1) { animation-delay: -0.32s; }
.ai-thinking-dot:nth-child(2) { animation-delay: -0.16s; }

@keyframes aiDotPulse {
  0%, 80%, 100% { transform: scale(0.6); opacity: 0.3; }
  40% { transform: scale(1.2); opacity: 1; }
}

.ai-thinking-text {
  font-size: 11.5px;
  color: var(--text-muted);
  font-style: italic;
}

/* Input Form */
.ai-input-form {
  padding: 12px 14px;
  border-top: 1px solid var(--border-subtle);
  background: #fafafa;
  display: flex;
  align-items: center;
  gap: 8px;
}

.ai-text-input {
  flex: 1;
  background: #ffffff;
  border: 1px solid var(--border-default);
  border-radius: 8px;
  padding: 8px 12px;
  font-size: 12.5px;
  color: var(--text-primary);
  outline: none;
  box-shadow: var(--shadow-subtle);
  transition: all 0.18s ease;
}

.ai-text-input:focus {
  border-color: var(--accent-black);
  box-shadow: 0 0 0 2px rgba(0, 0, 0, 0.08);
}

.ai-text-input::placeholder {
  color: var(--text-muted);
}

.ai-submit-btn {
  background: var(--accent-black);
  color: #ffffff;
  padding: 8px 14px;
  border-radius: 8px;
  font-size: 12px;
  font-weight: 600;
  border: none;
  cursor: pointer;
  box-shadow: var(--shadow-btn);
  transition: all 0.18s var(--ease-spring);
  white-space: nowrap;
}

.ai-submit-btn:hover:not(:disabled) {
  background: #27272a;
  transform: translateY(-1px);
  box-shadow: var(--shadow-btn-hover);
}

.ai-submit-btn:disabled {
  opacity: 0.45;
  cursor: not-allowed;
}

/* ---------- Minimalist Auth Screen ---------- */

.auth-canvas {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24px;
  background: radial-gradient(circle at 50% 10%, #f9fafb 0%, #ffffff 80%);
}

.auth-card {
  background: #ffffff;
  border: 1px solid var(--border-default);
  border-radius: var(--radius-lg);
  padding: 36px 32px;
  max-width: 380px;
  width: 100%;
  text-align: center;
  box-shadow: var(--shadow-modal);
  animation: fadeInUp 0.4s var(--ease-spring);
}

.auth-brand-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-size: 12px;
  font-weight: 600;
  color: var(--accent-black);
  background: var(--bg-muted);
  border: 1px solid var(--border-subtle);
  padding: 4px 12px;
  border-radius: 9999px;
  margin-bottom: 16px;
}

.auth-title {
  font-size: 20px;
  font-weight: 600;
  letter-spacing: -0.02em;
  color: var(--accent-black);
  margin-bottom: 6px;
}

.auth-subtitle {
  font-size: 12.5px;
  color: var(--text-muted);
  line-height: 1.5;
  margin-bottom: 22px;
}

.auth-google-btn {
